import 'package:flutter/material.dart';

class VoiceExpenseScreen extends StatelessWidget {
  const VoiceExpenseScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Voice Expense')),
      body: const Center(child: Text('Voice Recording - To be implemented')),
    );
  }
}
